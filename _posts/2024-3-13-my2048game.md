<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>2048 Game</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #faf8ef;
    }

    #game-container {
      background-color: #bbada0;
      padding: 20px;
      border-radius: 5px;
    }

    #grid-container {
      display: grid;
      grid-template-columns: repeat(4, 100px);
      grid-template-rows: repeat(4, 100px);
      gap: 10px;
    }

    .cell {
      width: 100px;
      height: 100px;
      background-color: #cdc1b4;
      border-radius: 5px;
      display: flex;
      justify-content: center;
      align-items: center;
      font-size: 24px;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div id="game-container">
    <div id="grid-container"></div>
    <div id="message">Use arrow keys to play!</div>
  </div>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const gridContainer = document.getElementById('grid-container');
      const message = document.getElementById('message');
      let grid = [];

      // Initialize the grid
      function initializeGrid() {
        grid = Array.from({ length: 4 }, () => Array(4).fill(0));
        generateNewTile();
        generateNewTile();
        renderGrid();
      }

      // Generate a new tile (2 or 4) in an empty spot on the grid
      function generateNewTile() {
        const emptyCells = [];
        grid.forEach((row, i) => {
          row.forEach((cell, j) => {
            if (cell === 0) {
              emptyCells.push({ row: i, col: j });
            }
          });
        });
        if (emptyCells.length > 0) {
          const { row, col } = emptyCells[Math.floor(Math.random() * emptyCells.length)];
          grid[row][col] = Math.random() < 0.9 ? 2 : 4;
        }
      }

      // Render the grid
      function renderGrid() {
        gridContainer.innerHTML = '';
        grid.forEach(row => {
          row.forEach(cell => {
            const cellElement = document.createElement('div');
            cellElement.classList.add('cell');
            cellElement.textContent = cell === 0 ? '' : cell;
            cellElement.style.backgroundColor = getCellColor(cell);
            gridContainer.appendChild(cellElement);
          });
        });
      }

      // Get color for cell based on value
      function getCellColor(value) {
        switch (value) {
          case 2: return '#eee4da';
          case 4: return '#ede0c8';
          case 8: return '#f2b179';
          case 16: return '#f59563';
          case 32: return '#f67c5f';
          case 64: return '#f65e3b';
          case 128: return '#edcf72';
          case 256: return '#edcc61';
          case 512: return '#edc850';
          case 1024: return '#edc53f';
          case 2048: return '#edc22e';
          default: return '#cdc1b4';
        }
      }

      // Handle keydown events
      document.addEventListener('keydown', (event) => {
        const direction = event.key.replace('Arrow', '').toLowerCase();
        moveTiles(direction);
        generateNewTile();
        renderGrid();
        if (gameOver()) {
          message.textContent = 'Game over! Press F5 to restart.';
        }
        if (checkWin()) {
          message.textContent = 'Congratulations! You won!';
        }
      });

      // Move tiles in the specified direction
      function moveTiles(direction) {
        switch (direction) {
          case 'up': // Implement move up logic
            break;
          case 'down': // Implement move down logic
            break;
          case 'left': // Implement move left logic
            break;
          case 'right': // Implement move right logic
            break;
        }
      }

      // Check if the game is over (no valid moves left)
      function gameOver() {
        // Implement game over logic
        return false;
      }

      // Check if the player has reached 2048
      function checkWin() {
        // Implement win logic
        return false;
      }

      // Initialize the grid when the page loads
      initializeGrid();
    });
  </script>
</body>
</html>
